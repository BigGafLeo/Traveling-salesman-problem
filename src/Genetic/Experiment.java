package Genetic;

import java.io.PrintStream;

/*
public class Experiment {
	public static void testAllParametersAlone(String name){
		String[] arr = name.split("\\.");
		Matrix matrix = FileManager.readFile("data/ALL_" + arr[1] + "/" + arr[0] + "." + arr[1]);
		int [][] results = new int[12][];
		int [] populationSize = {100,200,300};
		int [] islandsNumber = {2,4,6};
		GeneticAlgorithm geneticAlgorithm = new GeneticAlgorith(matrix, populationSize, islandsNumber);
		geneticAlgorithm.start(

	}
}
*/